# Vuori Path A gold — demo pack

**Source:** Gold clickthrough `/workspace/demos/vuori-path-a/` + Algolia cards `gladly-after-product-cards.json`.

**Journey:** Open → PRODUCT_LIST×3 (soft joggers under $120) → Navy refine (OOS + Black Heather alt) → Policy → Soft-close (no cart theater).

**Mount:** `demo.html?pack=vuori-path-a-gold` (progressive play default; do not pass `animate=0`).

| File | Role |
|------|------|
| `demo-spec.json` | Path A mount SoT — turns verbatim from gold HTML |
| `film-assets/` | Real product images (CDN + gold thumbs fallback) |
| `plate/pdp.png` | Firecrawl full-page screenshot of Performance Jogger PDP |
| `chat-widget/` | cw- CSS copied from `huckberry-fatigue-firecrawl` |
| `scrape/` | Firecrawl artifacts |

Soft-close honesty: `softClose: true` — no Added / Pay / ATC success UI.
